<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        ul{
            display: flex;
            flex-direction: column;
            gap: 20px;
        }
        li{
            font-size: 20px;
            word-spacing: 20px;
        }
    </style>
</head>
<body>
    <h1>Hello world</h1>
    <ul>
        <% allUrl.forEach(url => { %>
            <li> shortId: <%=url.shortId%>  redirectedUrl: <%= url.reDirectUrl %>  clicks : <%= url.visitHistory.length %> </li>
        <% }) %>
    </ul>
</body>
</html>